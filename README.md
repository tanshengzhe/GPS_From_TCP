Get GPS data through TCP (by IP)

1. Connect Novatel GNSS receiver through Ethernet.

2. Remember to change the IP and port number, and configure the log message needed in Novatel GNSS, using port ICOM1.

3. Run code.



Decoding part only decodes "$GPGGA", "BESTPOSA", and "INSPVAA" message.
