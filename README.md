Get GPS data through TCP (by IP)

1. Connect GNSS receiver through Ethernet.

2. Remember to change the IP and port number.

3. Run code.



Decoding part only decodes "$GPGGA" and "BESTPOSA" message.
